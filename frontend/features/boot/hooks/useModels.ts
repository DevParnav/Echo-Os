/**
 * @deprecated  useModels is no longer used.
 * The boot sequence now uses the unified useSystemStatus hook
 * which calls GET /api/system/status for all data.
 * This file is kept only for compilation safety.
 */
export {};
